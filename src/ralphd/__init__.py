# Kept in step with pyproject.toml's `version` by
# tests/test_packaging_metadata.py -- this literal is what `GET /version`,
# `ralphctl --version` and `ralphd-engine --version` report.
__version__ = "0.6.0"
API_VERSION = 1
