# v0.5 definition of done — evidence checklist (task 059)

Commit under test: `HEAD` at the time of the sweep = task 059's commit (the
sha is recorded in `git log` as `task 059: final verification sweep and
roadmap update`; the sweep below was run on its tree, working copy clean apart
from this report).
Host: ralphd job container, Python 3.12.13, ruff 0.16.2, docker socket
present, `playwright-cli` on PATH — so the `docker` and `browser` tiers really
execute here rather than skipping.

## 1. Verification sweep

```
$ .venv/bin/python -m ruff check .
All checks passed!

$ .venv/bin/python -m pytest -q
802 passed, 1 warning in 864.50s (0:14:24)
```

Tier composition of that run (no clean-skip needed on this host):

```
$ .venv/bin/python -m pytest -q --collect-only -m docker
8/802 tests collected (794 deselected)

$ .venv/bin/python -m pytest -q --collect-only -m browser
18/802 tests collected (784 deselected)
```

Raw log: `artifacts/reports/v0.5-final-suite.log` (verbatim pytest output; also
copied to the run dir as `artifacts/reports/final-suite.log`).

Baseline: the plan recorded 393 tests / 69 modules at plan HEAD (`5472453`);
the phase-1 gate (task 044) recorded 656. Tasks 045–059 added the phase-2
modules plus `tests/test_v05_definition_of_done.py` (this task).

## 2. Definition-of-done bullets and their evidence

Every bullet below is enforced by a test in the suite above, so the evidence
is re-checked on every run rather than being a one-off transcript.

### a. A 30-minute simulated outage costs zero iterations, zero approaches and zero tasks

| Evidence | What it proves |
|---|---|
| `tests/test_v05_definition_of_done.py::test_defaults_can_ride_out_a_thirty_minute_outage` | the *shipped defaults* (`2,5,15,30,60,120,300`s escalating, capped at `infra_retry_backoff_max_s=300`, budget `infra_outage_budget_s=4h`, no attempt cap) can absorb 30 minutes — the earlier retry tests all use a compressed schedule, this one asserts the real numbers |
| `tests/test_v05_definition_of_done.py::test_thirty_minute_outage_costs_no_iterations_approaches_or_tasks` | 12 consecutive faults ⇒ ≥1800s of (virtual) waiting on the default schedule, then recovery: same phase retried, `_infra_refunded == 12` so exactly **one** iteration is charged, `status.approach` unchanged, `tasks.json` byte-identical, deadline extended by exactly the waited time, episode clock reset |
| `tests/test_infra_outage_budget.py::test_five_consecutive_infra_faults_are_ridden_out_free` | the same claim end-to-end through the real engine + stub-pi: `state == succeeded`, `iterationsUsed == 3` (the happy path), `approach == 1`, `infra_retry` events 1..5 with the escalating/repeating backoff |
| `tests/test_inband_error_retry.py::test_inband_exit0_error_is_retried_refunded_and_job_completes` | the production-shaped fault (`exit 0`, zero tokens, in-band `errorMessage`) — the #11 bug — is retried and refunded |
| `tests/test_infra_deadline_extension.py` (5 tests) | `infraWaitTotalS` == Σ waits, `deadlineAt == startedAt + job_timeout_s + Σ waits`, per-episode accumulation, survives resume, untouched on a clean iteration |
| `tests/test_infra_retry_all_phases.py` | all five phases (planning/worker/review/verify/reflect) go through the wrapper and an infra fault consumes neither the verify error budget, nor the review steering budget, nor a task's `validationAttempts` |
| `tests/test_infra_outage_budget.py::test_outage_budget_exhaustion_ends_terminal_naming_the_duration` | the other side: a permanently dead endpoint still terminates, with a reason naming the outage duration, the budget spent waiting and the last error |

### b. …and is interruptible

| Evidence | What it proves |
|---|---|
| `tests/test_v05_definition_of_done.py::test_the_thirty_minute_wait_is_interruptible_by_retry_now` | a 300s default backoff is released in <5s wall clock by `retry_now()`, only the seconds actually spent are booked, the manual retry is an auditable event, and a second call with nothing to wake returns False (the 409 path) |
| `tests/test_retry_now.py` (5 tests) | the real route: `POST /retry` cuts a long backoff short (wall-clock assertion), resets the outage-budget episode clock, 409s when not waiting and on a finished job, and does **not** unpause a paused run or touch steering |
| `tests/test_cli_retry.py` (7 tests) | `ralphctl retry <run-id>` success/`--json`/409→exit 5/unknown run→3/unreachable→4, plus the docs guard |
| `tests/test_cli_ui.py::test_retry_proxy_*` (4 tests) | the hub's proxy route: forwards the token, passes the engine's 409 through, 503 for an unreachable run, 404 for an unknown one |
| `tests/test_browser_hub.py` degraded cases + `tests/test_status_health_infra_wait.py` | the operator can *see* the wait to interrupt it: `health: degraded` + populated `infraWait` mid-wait, both cleared after recovery, `infra_wait` events in the stream `ralphctl watch` follows, `ralphctl status`'s `degraded:` countdown line, and the hub's degraded card + retry-now button (absent when healthy) |

### c. A broken credential still fails in seconds, not after the outage budget

| Evidence | What it proves |
|---|---|
| `tests/test_instant_infra_retry.py::test_broken_credentials_still_fail_fast_not_after_the_outage_budget` | a missing/invalid credential (identical instant no-traffic failure every time) ends the run in seconds with the broken-environment diagnosis even though the outage budget is hours |
| `tests/test_instant_infra_retry.py::test_stable_instant_failures_fail_fast_with_the_broken_env_diagnosis`, `…::test_digits_only_differences_still_count_as_the_same_signature` | the carve-out's rule: `MAX_CONSECUTIVE_INSTANT_FAILURES` instant, no-traffic attempts with a *stable* signature (digits normalised) ⇒ stop |
| `tests/test_instant_infra_retry.py::test_instant_infra_fault_recovering_on_attempt_3_is_retried`, `…::test_instant_refused_connection_recovers_and_the_job_completes`, `…::test_varying_instant_signatures_never_trip_the_broken_env_verdict`, `…::test_a_reached_model_between_instant_faults_resets_the_streak` | the other direction: a genuinely transient instant fault (connection refused by a gateway still coming up) is retried and refunded instead of killing the run |
| `tests/test_no_progress_instant_failures.py` (3 tests) | the pre-existing fast-fail for instant planning/worker failures still burns no approaches, and the stagnation guard is unaffected |

### d. A killed container is detected by repair / status / hub, its logs are readable, and opt-in auto-resume brings it back

| Evidence | What it proves |
|---|---|
| `tests/test_cli_repair.py::test_repair_reports_dangling_container_for_running_run`, `…::test_repair_human_output_names_the_dangling_container`, `…::test_repair_set_state_aborted_writes_vanished_container_reason`, `…::test_repair_dangling_check_has_one_implementation`, `…::test_repair_still_refuses_dangling_check_on_live_container` | `repair` reports the condition (no longer "no issues found"), reusing doctor's single check implementation; `--set-state aborted` writes state + a vanished-container reason + an audit event; a live container is still refused |
| `tests/test_cli_repair.py::test_doctor_and_repair_recommend_the_same_next_command`, `…::test_dangling_remedy_text_has_one_implementation` | doctor's and repair's remedy tell one story, from one implementation |
| `tests/test_cli_status_dead_run.py` (12 tests) | `status` prints the container-gone warning line, relabels the frozen duration as time since last update, computes task counts from `tasks.json` with the engine's key mapping, carries both in `--json`, and leaves live/terminal output unchanged |
| `tests/test_cli_ui.py::test_dead_nonterminal_run_is_flagged_container_gone_in_list_and_detail`, `…::test_live_nonterminal_run_is_not_flagged_container_gone`, `…::test_hub_and_cli_share_one_nonterminal_state_set` + `tests/test_browser_hub.py` dead-run cases | the hub's warning treatment for a dead non-terminal run, absent for a healthy one, sharing one non-terminal state set with the CLI |
| `tests/test_cli_logs_dead_run.py` (10 tests), `tests/test_log_merge.py`, `tests/test_cli_ui.py::test_log_endpoint_falls_back_to_on_disk_merge_for_a_dead_run`, `…::test_log_endpoint_on_disk_lines_match_the_shared_merge`, `tests/test_no_transcript_message.py` | logs stay readable with the container gone: `ralphctl logs` pretty/`--raw`/`--follow`/`--tail`/single-iteration all exit 0 with a stderr snapshot notice, the hub tail falls back to the same shared merge with `live: false` and a snapshot label, identical lines from the live and on-disk paths, and `(no transcript yet)` for an empty run |
| `tests/test_cli_auto_resume.py` (15 tests) | `auto_resume` defaults **off** from a single literal (`AUTO_RESUME_DEFAULT`), settable per run (`--auto-resume`/`--no-auto-resume`), by registry default and by template, and it survives `resume` |
| `tests/test_cli_doctor_fix.py` (8 tests) | `doctor --fix` resumes exactly the opted-in dangling runs (asserted on the recorded `docker run` argv: mounts, env, labels reproduced), leaves opted-out ones untouched but reported, never touches a live container, plain `doctor` resumes nothing, cron/systemd deployment documented |
| `tests/test_cli_auto_resume_guard.py`, `tests/test_cli_auto_resume_operator_terminated.py` | the crash-loop guard (`autoResume: {attempts, lastAt, maxAttempts}`, escalating backoff, give-up reason readable from status) and the rule that terminal / operator-aborted / operator-stopped runs are never resurrected |

### e. `ralphctl watch` blocks on a resumed run instead of closing on a stale terminal marker

| Evidence | What it proves |
|---|---|
| `tests/test_cli_watch_resumed_run.py::test_watch_streams_past_historical_terminal_marker_on_resumed_run` | the #13 regression, named for the scenario: a mid-log terminal marker with later events does not end the stream — the follower blocks until the real terminus |
| `tests/test_cli_watch_resumed_run.py::test_watch_on_live_run_still_closes_on_its_own_terminal_event`, `…::test_watch_on_finished_resumed_run_closes_at_the_final_marker`, `…::test_trailing_log_events_after_the_terminal_marker_do_not_hold_the_stream` | live-run and already-finished-run behaviour unchanged (terminal + not live ⇒ close) |
| `tests/test_resume_state_event.py` | resume appends an explicit `running` state event, so a historical terminal marker can never be the log's final word |
| `tests/test_cli_logs_resumed_run.py`, `tests/test_cli_logs_follow_liveness.py` | the same liveness reconciliation audited and enforced on the `logs -f` / `GET /logs` path |

### f. The documented sibling-cleanup idiom cannot delete the job container

| Evidence | What it proves |
|---|---|
| `tests/test_sibling_cleanup_job_safe.py::test_documented_cleanup_removes_siblings_and_spares_the_job` | the *exact* documented command, run against a fixture with a labelled job container plus labelled siblings: siblings removed, job container survives |
| `tests/test_sibling_cleanup_job_safe.py::test_the_forbidden_form_really_does_delete_the_job_container` | the negative control — the old `--filter label=ralphd.run=…`-only form does destroy the job container, which is why the rule exists |
| `tests/test_sibling_cleanup_job_safe.py::test_ralphctl_stop_and_rm_still_reap_everything` | reaping remains ralphctl's job and is unchanged |
| `tests/test_cli_docker.py` argv assertions (task 034) | the job container carries `--label ralphd.role=job` and `RALPHD_SELF_CONTAINER_ID`; `_reap_siblings()` still filters on the run id alone |
| `tests/test_sibling_cleanup_guidance.py`, `tests/test_toolchain_sibling_guidance.py`, `tests/test_docs_consistency.py` | the rule *and its why* are in the rendered prompt, and the sibling-only filter is present in all four documented copies with no surviving run-label-only cleanup command anywhere in prompts/docs/examples |

## 3. Roadmap

`docs/roadmap.md`'s v0.5 block now records what actually shipped (every bullet
✅ with the concrete mechanism named) and its status header points at the three
evidence reports; the deferred list keeps the `auto_resume` default-flip note
(guarded by `tests/test_cli_auto_resume.py::test_roadmap_records_the_planned_default_flip_and_names_the_single_place`).

## 4. Traceability and issue hygiene

- `artifacts/reports/issue-traceability.md` maps #1–#11 and #13 → PRD
  requirement letter → task ids → commit shas → tests;
  `tests/test_issue_traceability.py` re-verifies every sha, path and node id
  and asserts that nothing in the tree or in git history closes a GitHub
  issue (closing them is the operator's call, not the run's).
- `artifacts/reports/phase1-gate.md` records the phase-1 gate, including the
  justification for every pre-existing test that was modified.
- `artifacts/reports/pricing-anomaly.md` records the #10 investigation.

## 5. Verdict

**PASS.** 802 tests pass with zero failures, zero skips and zero errors
(14:24 wall clock), including the 8 `docker`-marked container-level e2e tests
and the 18 `browser`-marked hub e2e tests; `ruff check .` is clean. The one
warning is a third-party `StarletteDeprecationWarning` from
`starlette.testclient`, not a ralphd deprecation. Every definition-of-done
bullet above has a named, currently-green test behind it, and every task in
the v0.5 plan (001-060) is completed. Remaining out-of-scope items are
unchanged and recorded in `docs/roadmap.md` (published Docker image + PyPI
packaging; the deferred list).
